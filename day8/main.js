/*
 Toán tử số học
 */

let a = 2;
let b = 5;

console.log("a + b = ", a + b);
console.log("a - b = ", a - b);
console.log("a * b = ", a * b);
console.log("a : b = ", a / b);
console.log("a ^ b = ", a ** b);
console.log("a % b = ", a % b);

/*
TOÁN TỬ SO SÁNH >, <, >=, <= , == 
KẾT QUẢ: BOOLEAN (TRUE HOẶC FALSE)
*/

console.log("Toán tử so sánh - quan hệ");

console.log("a > b: ", a > b);
console.log("a < b: ", a < b);
console.log("a <= b: ", a <= b);
console.log("a >= b: ", a >= b);
console.log("a == b: ", a == b);
console.log("a != b", a != b);

// ===: phải cùng giá trị và cùng loại
// true (1), false (0)
console.log(1 === true)
// 1.0 và 1


/*
TOÁN TỬ LOGIC: AND  (VÀ), OR (HOẶC), NOT (PHỦ ĐỊNH)
VD: nếu trời không mưa và có lịch học thì học thể dục bình thường.
    (Thời tiết != mưa) && (lichHoc == true)

VD: Nếu trời mưa to hoặc có tuyết -> học thể dục trong nhà.
   (thời tiết == mưa to) || (có tuyết == true)

*/
